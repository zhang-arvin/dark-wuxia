# 秘境系统重构方案 v2.0

## 现状问题
- 每个难度只有1关，打完就退出了
- 无选择节点，玩家体验单调
- 无秘境进度奖励
- 无难度递推机制

## 方案：每秘境 20-30 步抉择制（参考 D2地图探索 + 文字Roguelite）

### 1. 秘境流程

```
进入秘境 → 随机生成 N 步路径 (20-30步，随难度变化)
  每一"步"是一个事件节点，类型随机：
  
  ├─ 战斗节点 (~60%) — 遇敌战斗
  ├─ 事件节点 (~20%) — 二选一选择：
  │   ├─ 岔路口：左=保守(无风险少量奖励) / 右=冒险(高风险高奖励)
  │   ├─ 机关：躲避(概率失败扣血) / 硬闯(必扣血但得宝箱)
  │   └─ 营地：休息(回血回内力) / 搜刮(得银两或装备)
  ├─ 宝箱节点 (~10%) — 直接开箱得装备
  ├─ 奇遇节点 (~10%) — 随机正负事件
  
  步数走完 → 遇Boss (仅当步数≥80%时, 即Boss固定在第N步)
  Boss胜利 → 秘境完成 → 结算奖励
```

### 2. 难度递推 + 秘境解锁

```
秘境难度递推：
  古墓幽影 normal 通关 → 古墓幽影 hard 解锁
  古墓幽影 hard 通关 → 古墓幽影 hell 解锁
  古墓幽影 hell 通关 → 古墓幽影 inferno 解锁
  
  通关古墓幽影 inferno → 剑冢 normal 解锁
  剑冢 normal 通关 → 剑冢 hard 解锁
  ...依次类推
```

### 3. 完成奖励

```
第一次通关某难度:
  - 银两 200 × 难度系数
  - 保底橙色+装备1件
  - 福缘 +2
  - 记录"已通关"状态

重复刷:
  - 银两 50 × 难度系数
  - 正常掉落(按掉落表)
```

### 4. 步数设计

| 难度 | 步数 | 中途事件 | Boss位置 |
|------|------|----------|----------|
| normal | 20 | 1机关+1营地 | 第20步 |
| hard | 24 | 2机关+1营地 | 第24步 |
| hell | 28 | 3机关+1营地 | 第28步 |
| inferno | 30 | 3机关+2营地 | 第30步 |

### 5. 事件选择示例

**岔路口**："前方出现两条路，左边的山道安全但无惊喜，右边的断崖风险极高但崖下似乎有宝光闪烁。"
- 左（安全）: 无事发生，继续前进
- 右（冒险）: 50% 获得珍贵宝箱，50% 扣除 20% 生命

**机关**："一道暗箭机关横在路中。"
- 躲避: 身法判定，失败扣 30% 生命
- 硬闯: 必扣 20% 生命，但地上有个宝箱

**营地**："远处有火光，似乎是一个商旅营地。"
- 休息: 恢复全部生命和内力
- 搜刮: 获得银两或一件装备，但营地的人会不高兴（下次事件概率变差）

## 存储改动

1. `realm_progress` 表添加:
   - `currentStep` — 当前步数
   - `cleared` — 是否首通
   - `unlockedRealms` — 已解锁秘境列表
   
2. 新增 `realm_unlock` 表:
   - `realmId` + `difficulty` — 解锁状态
   - 首次通关时间

## 实施顺序

1. **事件系统** — 事件节点 + 选择逻辑 (Phase 1)
2. **步数系统** — 每秘境生成N步路径 (Phase 1)
3. **Boss阶梯** — Boss固定最后一步 (Phase 2)
4. **完成奖励** — 首通/重复奖励 (Phase 2)
5. **解锁递推** — 数据库解锁状态 (Phase 3)