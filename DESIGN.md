# 暗黑武侠 · 方案设计文档 v1.0

> 项目名：暗黑武侠（暂定）
> 类型：文字+图片 · 暗黑式刷装 · 武侠题材
> 平台：Flutter → iOS/Android → TapTap 首发
> 商业模式：买断制 6-12 元
> 文档版本：v1.0 | 2026-09-06

---

## 一、项目概述

### 1.1 一句话定位
「暗黑破坏神2 的刷装 BD 深度 × 武侠的经脉/走火入魔策略 × 文字为主+图片辅助的轻量呈现」

### 1.2 核心循环
```
选秘境 → 推进 → 战斗(分档) → 掉落(闪光) → 回城休整 → 配BD → 刷更深难度
```

### 1.3 设计原则
1. **文字为主，图片为辅** — 关键节点(掉落/Boss/暗金)配图，日常战斗纯文字
2. **连刷模式补偿延迟** — 碾压内容自动结算1句话，不拖慢刷的节奏
3. **走火入魔不惩罚刷** — 改为BD负面协同(经脉相克)，是配装时的选择不是刷时的惩罚
4. **LLM 预生成不实时** — 200-500条战斗描写模板，运行时随机+变量填充，零API成本
5. **数据驱动** — 装备/武功/经脉/事件/秘境全部JSON配置，代码只管引擎逻辑
6. **SQLite 存档** — 不用JSON全量序列化，装备表分页查询
7. **买断制无内购** — 完整内容+离线可玩+无广告

---

## 二、世界观

### 2.1 文化公共 IP（非金庸专属）
使用历史真实存在的门派和文化共识概念：

| 门派 | 特色 | 武功方向 |
|------|------|---------|
| 少林寺 | 佛门刚猛 | 拳法/外功 |
| 武当派 | 道家柔化 | 剑法/内功 |
| 丐帮 | 市井江湖 | 擒拿/掌法 |
| 峨眉派 | 刚柔并济 | 剑法/刺 |
| 点苍派 | 西域风格 | 刀法/暗器 |
| 天机阁(架空) | 情报组织 | 暗器/轻功 |
| 幽冥教(架空) | 邪道 | 毒功/邪术 |
| 隐仙谷(架空) | 避世高人 | 奇门/医术 |

### 2.2 主线叙事线索
「为寻找失传的《太玄经》，探索各地秘境，对抗暗中操控武林的幽冥教」

### 2.3 时代背景
架空朝代「天启年间」，江湖纷争，幽冥教暗中渗透各大门派。

---

## 三、核心系统设计

### 3.1 角色系统

#### 3.1.1 五维属性
```
臂力(BODY)   — 外功伤害基数、生命上限
身法(AGI)    — 先手值、闪避率、暴击率
悟性(WIS)    — 修炼速度、武功兼容性
根骨(CON)    — 内力上限、内力恢复、生命恢复
福缘(LUCK)   — 掉落率加成(=D2的MF)、奇遇触发率
```

#### 3.1.2 衍生属性
```
战力指数(POWER) — 综合评分，一键看成长
内力(INNER)     — 根骨×等级×内功加成
生命(HP)        — 臂力×等级×装备加成
先手值(INIT)    — 身法×轻功加成
闪避(DODGE)     — 身法×轻功×经脉加成
```

### 3.2 战斗系统（分档制）

#### 3.2.1 三档战斗
| 档位 | 触发条件 | 回合数 | 文字量 | 说明 |
|------|---------|--------|--------|------|
| 碾压 | 敌人战力<玩家70% | 0-1 | 1句话 | 连刷模式自动结算 |
| 正常 | 敌人战力70%-120% | 5-8 | 3-5段 | 标准回合制 |
| Boss | 精英/Boss | 10-15 | 展开式 | 完整招式交锋描写 |

#### 3.2.2 回合内多动作
```
1回合 = 1轮
1轮 = 双方各做 2-3 个动作（出招/运功/闪避/反击）
→ 5回合 = 10-15个动作，BD表达空间充足
```

#### 3.2.3 连刷模式
```
玩家对已碾压秘境开启连刷：
  ├ 自动战斗 → 1句话结算 → 掉落展示 → 继续推进
  ├ 只在掉落暗金+以上时停顿展示
  ├ 可随时中断回城
  └ 模拟D2"跑牛关"的快速循环
```

#### 3.2.4 掉落闪光
```
凡品/良品: 简短文字 "> 获得【良品】精钢剑"
上品:      带词条 "> ★【上品】寒铁剑 | 外功+15% 破甲"
暗金:      仪式感停顿 + 完整描述 + 配图
神品/传说: 全屏文字特效 + 故事化掉落叙事 + 配图
```

### 3.3 装备系统

#### 3.3.1 品质分级
| 品质 | 掉率 | 词缀数 | 特殊 | 颜色标记 |
|------|------|--------|------|---------|
| 凡品 | 55% | 1-2 | - | 白 |
| 良品 | 28% | 2-3 | 1附加 | 蓝 |
| 上品 | 12% | 3-4 | 套装属性 | 黄 |
| 暗金 | 3% | 固定独特+2-4随机 | 改机制 | 橙★ |
| 神品 | 0.5% | 多条改机制 | 主题词缀 | 红★★ |
| 传说 | 0.1% | 改变玩法规则 | - | 金★★★ |

#### 3.3.2 保底机制
```
运气积累: 每刷N只怪未掉暗金+ → 暗金掉率递增
保底: 连续刷200只未掉暗金 → 第201只必掉暗金
```

#### 3.3.3 词缀系统
```yaml
前缀:
  - "锋利的": 外功伤害+10-20%
  - "嗜血的": 击杀回复生命5%
  - "破甲的": 无视部分内功防御
  - "阴寒的": 附加冰属性伤害
  - "刚猛的": 外功暴击+15%
后缀:
  - "of 力量": 臂力+3-8
  - "of 悟性": 悟性+3-8
  - "of 福缘": 福缘+5-15%(掉率加成)
  - "of 根骨": 内力上限+10-30%
```

#### 3.3.4 装备槽位
```
weapon    兵器 (剑/刀/枪/鞭/扇/珠)
armor     护体 (布衣/软甲/金丝甲/天蚕丝衣)
accessory 饰品 (玉佩/毒珠/暗器匣/护心镜)
treasure  奇物 (丹药/卷轴，消耗品)
```

#### 3.3.5 暗金设计原则
- 每件暗金有**改机制独特词条**，不是简单数值
- 数量目标：首发30-50件，后续扩充至100+
- 低级暗金也有长期价值（改机制不依赖数值）

#### 3.3.6 套装系统
```yaml
分层加成:
  2件: 小数值加成
  4件: 机制改变
  6件: 大幅机制改变

设计原则:
  - 套装非最优解，混搭暗金也能最强
  - 多维同源: 同门派/同兵器/同时代遗物
```

#### 3.3.7 掉落回收
```
不需要的装备 → 熔炼成材料 → 用于强化/重铸自己需要的装备
解决了"掉了不需要=浪费"的体感问题（D2靠交易解决，我们靠系统回收）
```

### 3.4 BD 流派系统（4维）

#### 3.4.1 四维定义
| 维度 | 角色 | 类比D2 | 说明 |
|------|------|---------|------|
| 内功 | 根基 | 属性/被动 | 决定基础数值和属性方向，影响外功兼容性 |
| 外功 | 招式 | 主动技能 | 包含轻功(身法招式)，主动行为选择 |
| 装备 | 词条 | 装备+符文 | 含经脉镶嵌子系统 |
| 心法 | 改规则 | runeword效果 | 改变规则的特殊被动，非数值加成 |

#### 3.4.2 内功兼容性
```
阳刚型 → 兼容刚猛外功，不兼容阴柔外功(相克→真气逆行风险)
阴柔型 → 兼容快剑/暗器，不兼容阳刚拳法
中和型 → 全兼容(极稀有)
```

#### 3.4.3 心法示例
```yaml
- name: "以柔克刚"
  effect: 外功伤害转化为内功伤害计算
  # 改规则！不是数值加成

- name: "先发制人"
  effect: 先手值高的角色第一回合额外行动
  
- name: "杀意"
  effect: 杀人越多伤害越高(每击杀+5%伤害，上限+50%)

- name: "天人合一"
  effect: 经脉相克不再触发真气逆行(解锁双修流)
```

#### 3.4.4 BD 流派画像
系统分析当前BD，生成流派名称和画像描述：
```
你的武学走的是【刚猛一路】
内功浑厚、外功霸道、不善轻功
经脉走少阳纯阳一路
适合正面硬撼，弱于闪避反击
```

#### 3.4.5 敌人机制多样性（防BD趋同）
```
免疫物理Boss → 需要内功流
反击近战Boss → 需要轻功远程流
持续回血Boss → 需要爆发秒杀流
多阶段Boss  → 需要续航+适应流
```

### 3.5 经脉系统（=D2符文之语）

#### 3.5.1 简化设计
```
6条经脉（3阳3阴），每条5个穴位 = 30穴位
命名: 阳脉1穴/阳脉2穴... 不用真实中医名
```

#### 3.5.2 真气种子（=符文）
```
掉落物: 真气种子（类似D2符文掉落）
操作: 把真气种子"种入"对应穴位（拖放式操作）
效果: 不同种子对应不同穴位加成
```

#### 3.5.3 经脉之语（=符文之语）
```
特定穴位按特定顺序种入真气种子 → 解锁隐藏效果
例: "任督贯通" = 会阴→气海→膻中→天突→百会→玉枕→命门 → 内力恢复x2
```

#### 3.5.4 配方发现
```
不纯靠摸索，不全公开:
  - 掉落"经脉残卷"描述部分配方线索
  - 收集残卷拼出完整配方
  - 增加探索乐趣又降低门槛
```

#### 3.5.5 可视化
```
ASCII经脉图展示已打通穴位和网络
比纯文字描述直观10倍
```

### 3.6 真气逆行系统（原走火入魔）

#### 3.6.1 设计原则
```
❌ 不惩罚刷的频率（原来的设计是反设计）
✅ 惩罚BD的不匹配（配装时的选择）
```

#### 3.6.2 触发条件
```
经脉相克: 同时激活相克经脉(如少阳+少阴)
内功外功冲突: 阳刚内功强行配阴柔外功
强制高阶: 熟练度不够强行使用高阶武功
```

#### 3.6.3 效果
```
轻度: 内力恢复-30%，偶尔出招失误
中度: 随机负面效果，属性波动
重度: 每回合损血，但有属性暴增(高风险高收益路线)
```

#### 3.6.4 解除方式
```
- 回城休整(安全但花时间)
- 服用定心丹(消耗品)
- 特定内功自动压制
- 心法"天人合一"可完全免疫(解锁双修流)
```

#### 3.6.5 双修流（高风险高收益BD）
```
玩家可选: 同时走阳+阴经脉
  → 触发真气逆行但属性暴增
  → 需要用心法/装备/丹药来压制副作用
  → 这是有趣的BD构建选择，不是惩罚
```

### 3.7 秘境系统

#### 3.7.1 秘境类型
| 类型 | 结构 | 适合 |
|------|------|------|
| 线性秘境 | 4层递进，Boss在最后 | 叙事 |
| 开放秘境 | 1大区域，多个房间随机，Boss随机 | 刷装(类似牛关) |
| 挑战秘境 | 1层1个超强Boss | BD验证 |
| 迷宫秘境 | 多层分支路径，选择影响掉落 | 策略性 |

#### 3.7.2 难度分级（改规则非纯数值）
| 难度 | 改变 |
|------|------|
| 普通 | 标准体验 |
| 困难 | 敌人有额外词条(反伤/嗜血) |
| 地狱 | 敌人机制改变(Boss新增二阶段) |
| 炼狱 | 规则改变(禁轻功/内力持续流失) |

#### 3.7.3 秘境变体
```
同一秘境有变体(参数化生成):
  - 普通(标准)
  - 被诅咒的(敌人强化但掉落更好)
  - 古老的(有额外机制如时间限制)
→ 1个秘境设计成本扩展3-5种体验
```

#### 3.7.4 首发秘境列表
| 秘境 | 类型 | 特色 | Boss掉落 |
|------|------|------|---------|
| 古墓遗迹 | 线性 | 新手友好 | 少林系暗金 |
| 剑冢 | 开放 | 剑法BD圣地 | 剑系暗金 |
| 藏经洞 | 线性 | 内功BD圣地 | 内功系暗金 |
| 幽冥鬼蜮 | 迷宫 | 邪功BD圣地 | 邪功系暗金 |
| 龙脉深处 | 挑战 | 最难，掉落最好 | 神品掉落 |

#### 3.7.5 定向掉落
```
每个秘境Boss有专属掉落表
玩家可以"去剑冢刷那把特定的剑"
比纯随机更符合文字游戏的探索感
```

### 3.8 城镇枢纽

#### 3.7.1 功能
```
客栈/门派枢纽:
  ├ 仓库(查看/管理装备，分页加载)
  ├ 炼器(合成/强化/重铸)
  ├ 天机阁(赌博: 花银两赌装备)
  ├ 药铺(买药/解毒/定心丹)
  ├ NPC(接任务/给线索/讲江湖传闻)
  ├ 配BD(换装备/经脉/心法)
  ├ 概率公示页
  ├ 排行查看(每日种子排行)
  └ 休息(恢复全部状态)
```

### 3.9 辅助系统

#### 3.9.1 福缘系统（=D2 MF）
```
福缘属性影响掉落率
可牺牲战力堆福缘("用更弱BD换更高掉率")
装备有"of 福缘"后缀(+5-15%掉率)
复刻D2 MF的期待感
```

#### 3.9.2 天机阁赌博（=D2 Gambling）
```
花银两赌装备
小额保底(必出良品)
小概率出极品(暗金/神品)
给非酋玩家一条出路
```

#### 3.9.3 炼器合成（=D2 Horadric Cube）
```
低品质装备 + 材料 → 合成更高品质
给非酋玩家确定性路径
3件良品 + 熔炼材料 = 1件上品(保底)
```

#### 3.9.4 无尽模式 + 每日种子
```
无尽模式: 秘境层数无限，难度递增，排行榜记录最深层数
每日种子: 每天一个随机种子，排行榜按种子排名
替代赛季制，零内容更新成本但提供持续动力
```

#### 3.9.5 排行榜
```
秘境排行榜: 通关记录(回合数/BD流派)
  "我的刚猛流刷剑冢用了5回合，全服第一是3回合"
无尽模式排行: 最深层数
每日种子排行: 当日种子排名
```

#### 3.9.6 概率公示页
```
内建概率公示(JSON配置驱动)
每件装备/品质显示精确掉率
审核必备项(App Store Guideline 4.7)
```

### 3.10 成长可视化

#### 3.10.1 战力指数
```
综合"战力"数值，一键看成长
每次装备/BD调整，战力数值变化
```

#### 3.10.2 装备 diff 面板
```
换装备时:
  当前装备 → 候选装备
  内力  120 → 145 (+25)
  外功  35  → 28  (-7)
  经脉  √少阳 → ×无
  战力  1850 → 2100 (+250) ↑
```

#### 3.10.3 成长曲线图
```
角色关键属性随时间变化的曲线(ASCII图表)
每次更新装备/升级，曲线上跳
```

#### 3.10.4 招式描写随战力进化
```
同一招式，战力低: "你挥出一拳，打中了对手"
战力高: "你一拳打出，拳风呼啸，对手连退三步，口吐鲜血"
文字本身的变化反映成长
```

---

## 四、技术架构

### 4.1 技术栈
```
框架: Flutter (Dart)
状态管理: Riverpod 或 Bloc
存档: SQLite (sqflite + drift)
KV存储: shared_preferences (小数据)
路由: go_router
UI组件: Material + 自定义武侠风格
图片: flutter_cache_manager (缓存)
状态机: 真气逆行/经脉用状态模式建模
配置: 远程JSON配置(CDN) + 本地fallback
```

### 4.2 存档架构
```
SQLite (结构化大数据):
  - equipment (装备表，分页查询，每次50条)
  - run_history (秘境记录)
  - modifiers (词缀记录)
  - meridian_progress (经脉进度)
  - event_log (事件日志)

shared_preferences (快速KV):
  - player_state (当前角色状态)
  - settings (游戏设置)
  - session_cache (会话缓存)

文件系统:
  - 存档截图/分享图
  - 图片缓存

背包软上限: 500件，超限提示熔炼/出售
```

### 4.3 数据驱动架构
```
所有游戏内容用JSON配置:
  /config/
    ├── equipment_base.json    # 装备基础数据
    ├── affixes.json           # 词缀池
    ├── martial_arts.json      # 武功数据
    ├── meridians.json         # 经脉+经脉之语
    ├── secret_realms.json     # 秘境配置
    ├── events.json            # 事件库
    ├── enemies.json           # 敌人数据
    ├── bosses.json            # Boss数据
    ├── drop_tables.json       # 掉落表
    └── narration_templates.json # 战斗描写模板
```

### 4.4 战斗描写模板（预生成）
```
200-500条预写模板，分类:
  - 碾压档: "你一招{skill}，{enemy}应声倒地" (50条)
  - 正常档: 分招式类型×场景 (200条)
  - Boss档: 分Boss×阶段 (50条×变量)
运行时: 随机选取 + 变量填充({skill}/{enemy}/{damage})
LLM只用于后台批量生成模板，不在运行时调用
```

### 4.5 离线/在线设计
```
核心循环: 完全离线可玩
可选联网:
  - 云存档同步 (op-based log 增量同步)
  - 排行榜 (Cloudflare Workers + KV)
  - 远程配置拉取 (新装备/新秘境)

同步策略:
  装备库存: 追加合并(by id，无冲突)
  经脉进度: Last-Write-Wins + 版本号
  秘境进度: Last-Write-Wins
  角色属性: 从基础数据重算
```

### 4.6 图片资源管理
```
格式: WebP (比PNG小30%)
内置: UI装饰 + 品质边框 + 基础装备图标 (10-20MB)
按需下载: 秘境场景图 + 敌人图 (首次进入时下载)
CDN manifest: assets_manifest.json (url+size+hash)
AI生成: Stable Diffusion生成武侠风格 + 后处理统一风格
```

### 4.7 审核合规
```
概率公示页: 内建(JSON配置驱动)
暴力描写: 武侠小说风格非血腥写实
"走火入魔" → "真气逆行"(中性化)
LLM输出: 接阿里云/腾讯云内容安全API过滤
首发: TapTap(审核相对友好)
App Store: 关闭LLM功能版
```

---

## 五、数据结构（Dart 类定义）

### 5.1 核心数据模型

```dart
// 角色状态（每世临时）
class Character {
  String name;
  String origin;              // 出身
  int age;
  int level;
  Attributes attributes;       // 五维属性
  List<MartialArt> martialArts; // 武功列表
  EquipmentLoadout equipment;  // 当前装备
  List<MeridianNode> meridians; // 经脉状态
  String? heartMantra;         // 心法ID
  int health;
  int innerEnergy;
  int fortune;                 // 福缘(掉率加成)
  int reputation;              // 名望
  int alignment;               // 正邪值 -100~100
  int powerIndex;              // 战力指数
}

class Attributes {
  int body;    // 臂力
  int agi;     // 身法
  int wis;     // 悟性
  int con;     // 根骨
  int luck;    // 福缘
}

// 装备
class Equipment {
  String id;
  String baseId;               // 基础装备ID(指向config)
  Quality quality;             // 品质
  String name;                 // 生成名(含词缀)
  List<Affix> affixes;          // 词缀列表
  int reinforceLevel;          // 强化等级
  String? meridianSeedId;      // 镶嵌的真气种子
  int itemLevel;               // 物品等级
}

enum Quality { normal, magic, rare, unique, divine, legendary }

// 武功
class MartialArt {
  String id;
  String name;
  MartialType type;            // 内功/外功/轻功
  int proficiency;             // 0-100 熟练度
  ProficiencyLevel level;      // 初窥/入门/小成/大成/化境
  String elementAffinity;      // 阳刚/阴柔/中和
}

enum ProficiencyLevel { novice, beginner, minor, major, mastery }

// 经脉
class MeridianNode {
  String meridianId;           // 经脉ID(阳脉1/阴脉2等)
  int nodeIndex;               // 穴位序号
  bool isOpen;                 // 是否打通
  String? seedId;              // 种入的真气种子
}

// 秘境进度
class RealmProgress {
  String realmId;
  int currentLayer;
  int currentDifficulty;
  List<String> completedEvents;
  int enemyCount;
  int dropCount;
}

// 掉落记录
class DropRecord {
  String equipmentId;
  String realmId;
  int layer;
  DateTime timestamp;
}
```

### 5.2 配置数据（JSON）

```dart
// 装备基础配置
class EquipmentBase {
  String id;
  String name;
  EquipmentSlot slot;          // weapon/armor/accessory/treasure
  int baseDamage;              // 基础伤害/防御
  int itemLevel;               // 物品等级
  List<String> affixPool;      // 可用词缀池ID
  String? setImage;            // 所属套装ID
}

// 暗金配置
class UniqueEquipment {
  String id;
  String name;
  String description;          // 故事化描述
  int itemLevel;
  EquipmentSlot slot;
  List<UniqueEffect> uniqueEffects; // 改机制词条
  List<AffixRange> randomAffixes;   // 随机词缀范围
  Map<String, int> requirements;     // 臂力/正邪值等要求
}

class UniqueEffect {
  String id;
  String description;          // "无视内功防御"
  EffectType type;
  Map<String, dynamic> params;  // 参数
}

// 经脉之语
class MeridianWord {
  String id;
  String name;
  String description;
  List<MeridianSeedSlot> sequence;  // 要求的种子序列
  String effect;                     // 效果描述
  Map<String, int> bonuses;          // 属性加成
}

// 战斗描写模板
class NarrationTemplate {
  String id;
  BattleTier tier;             // 碾压/正常/Boss
  String template;             // "你一招{skill}，{enemy}应声倒地"
  List<String> requiredVars;   // ["skill","enemy","damage"]
  int weight;                  // 抽取权重
}

// 掉落表
class DropTable {
  String id;
  Map<Quality, double> rates;  // 品质掉率
  List<String> equipmentPool;   // 可掉装备ID列表
  int fortuneMultiplier;        // 福缘影响系数
  int pityCount;                // 保底计数器
}
```

---

## 六、开发路线

### Phase 1：MVP 验证（1-2 个月）

**目标**：在手机上跑通"刷→掉→配→刷更深"核心循环

| 模块 | 内容 | 优先级 |
|------|------|--------|
| Flutter项目搭建 | 项目结构+依赖+路由 | P0 |
| SQLite存档架构 | 表结构+DAO+分页查询 | P0 |
| 角色系统 | 五维属性+等级+战力指数 | P0 |
| 战斗系统 | 分档制(杂兵1回合/精英5-8/Boss10-15)+连刷模式 | P0 |
| 掉落系统 | 6品质+词缀随机+保底+闪光特效 | P0 |
| 装备管理 | 装备/卸装备/diff面板+背包分页 | P0 |
| 1个秘境 | 古墓遗迹(4层+Boss机制战) | P0 |
| 客栈枢纽 | 仓库+装备+简单BD | P0 |
| 概率公示页 | JSON驱动 | P0 |
| 数据配置 | 20件装备+1个秘境+1个BD路径 | P0 |

### Phase 2：BD深度+内容（2-3 个月）

| 模块 | 内容 | 优先级 |
|------|------|--------|
| 经脉系统 | 6脉30穴+真气种子+经脉之语+ASCII图 | P1 |
| 套装系统 | 分层2/4/6件+非最优解设计 | P1 |
| 暗金装备 | 30-50件改机制词条 | P1 |
| 心法系统 | 改规则被动 | P1 |
| 真气逆行 | BD负面协同+双修流 | P1 |
| 福缘+天机阁+炼器 | MF+赌博+合成 | P1 |
| 5个秘境+变体 | 参数化生成 | P1 |
| 战力指数+成长曲线 | 可视化 | P1 |
| 预生成模板200条 | 战斗描写 | P1 |
| BGM+音效 | 最廉价沉浸手段 | P1 |
| 掉落回收 | 熔炼系统 | P1 |

### Phase 3：上架准备（1-2 个月）

| 模块 | 内容 | 优先级 |
|------|------|--------|
| 无尽模式+每日种子 | 替代赛季 | P2 |
| 秘境排行榜 | 回合数/BD流派 | P2 |
| 图片资源 | AI生成+后处理 WebP | P2 |
| 内容安全过滤 | 如用LLM | P2 |
| B站开发日志 | 获客 | P2 |
| TapTap上架 | 聚光灯计划 | P2 |
| 买断制6-12元 | - | P2 |
| App Store(后续) | 关闭LLM版 | P3 |

---

## 七、任务拆分计划（子代理执行）

### 批次1：项目基础设施
- Task 1.1: Flutter项目初始化+依赖配置+目录结构
- Task 1.2: SQLite存档架构(表结构+DAO+drift)
- Task 1.3: JSON配置加载器(远程+本地fallback)

### 批次2：核心数据模型
- Task 2.1: Dart数据类定义(全部model)
- Task 2.2: 装备配置JSON(20件+词缀池)
- Task 2.3: 武功/经脉/心法配置JSON

### 批次3：战斗系统
- Task 3.1: 战斗引擎(分档制+回合内多动作)
- Task 3.2: 连刷模式+自动结算
- Task 3.3: 掉落系统(概率+保底+词缀随机)
- Task 3.4: 战斗描写模板系统(预生成+变量填充)

### 批次4：UI层
- Task 4.1: 角色创建+属性面板+战力指数
- Task 4.2: 秘境选择+推进+战斗界面
- Task 4.3: 掉落展示(闪光特效+仪式感)
- Task 4.4: 客栈枢纽(仓库+装备+BD面板+diff)
- Task 4.5: 概率公示页

### 批次5：内容
- Task 5.1: 古墓秘境(4层+Boss+事件+掉落表)
- Task 5.2: 20件装备数据(含5件暗金)
- Task 5.3: 战斗描写模板(50条起步)

### 批次6：Review
- 子代理1: 技术review(架构/性能/存档)
- 子代理2: 设计review(玩法/平衡/UX)
- 子代理3: 产品review(上架/审核/商业化)
